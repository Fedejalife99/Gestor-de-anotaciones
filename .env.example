# Copiá este archivo como .env y completá los valores para desarrollo local
# En producción (Render) estas variables se configuran desde el dashboard

# Si querés usar PostgreSQL localmente en vez de SQLite, descomentá y completá:
# DATABASE_URL=postgresql://usuario:password@localhost:5432/anotaciones
